const KEY='servertest_contabilidad_v3';
const OLD='servertest_contabilidad_v2';
const DIRECTUS_URL='https://directus.agrupacionnothofagus.cl';
const DIRECTUS_AUTH_KEY='teurgia_directus_auth';
const SERVER_COLLECTION='contabilidad_datos';
const SERVER_KEY='contabilidad_general';
const SERVER_ID_KEY='servertest_contabilidad_record_id';
const THEME_KEY='teurgia_contabilidad_modo_oscuro';
const $=id=>document.getElementById(id);
const n=v=>Number(v||0);
const money=v=>n(v).toLocaleString('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0});
const uid=()=>crypto.randomUUID?crypto.randomUUID():'id'+Date.now()+Math.random();
const today=()=>new Date().toISOString().slice(0,10);
const month=()=>new Date().toISOString().slice(0,7);
const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');
function visualAssetPrefix(){return location.pathname.endsWith('/contabilidad/')||location.pathname.endsWith('/contabilidad/index.html')?'assets/':'../assets/'}
function ensureVisualModeCss(){if(document.getElementById('premium-visual-mode-css'))return;let link=document.createElement('link');link.id='premium-visual-mode-css';link.rel='stylesheet';link.href=visualAssetPrefix()+'visual-mode.css?v=20260625h';document.head.appendChild(link)}
function aplicarModoVisual(){ensureVisualModeCss();if(localStorage.getItem(THEME_KEY)==='1')document.body.classList.add('dark');else document.body.classList.remove('dark');syncModoVisualLabel()}
function toggleModoVisual(){ensureVisualModeCss();document.body.classList.toggle('dark');localStorage.setItem(THEME_KEY,document.body.classList.contains('dark')?'1':'0');syncModoVisualLabel()}
function syncModoVisualLabel(){let light=document.body.classList.contains('dark');document.querySelectorAll('button').forEach(btn=>{if((btn.getAttribute('onclick')||'').includes('toggleModoVisual'))btn.innerHTML=light?'◑ Modo oscuro':'◐ Modo claro'})}
function ensureModoVisualButton(){let exists=[...document.querySelectorAll('button')].some(b=>(b.getAttribute('onclick')||'').includes('toggleModoVisual'));if(exists){syncModoVisualLabel();return}let top=document.querySelector('.top-actions')||document.querySelector('.auth-mini');if(!top){let bar=document.querySelector('.topbar')||document.querySelector('header')||document.body;top=document.createElement('div');top.className='top-actions';bar.prepend(top)}let btn=document.createElement('button');btn.className=top.classList.contains('auth-mini')?'visual-btn':'top-action';btn.type='button';btn.setAttribute('onclick','toggleModoVisual()');btn.innerHTML='◐ Modo claro';top.prepend(btn);syncModoVisualLabel()}
const SIDEBAR_ITEMS=[['','◻','Panel'],['atenciones','◉','Atenciones'],['caja','⌂','Caja'],['deudas','⦿','Deudas'],['liquidaciones','⇄','Pagos prof.'],['egresos','↘','Egresos'],['usuarios','☺','Usuarios'],['profesionales','★','Profesionales'],['aranceles','◆','Aranceles'],['reportes','▦','Reportes']];
function contabilidadRootPrefix(){return location.pathname.endsWith('/contabilidad/')||location.pathname.endsWith('/contabilidad/index.html')?'./':'../'}
function currentSectionKey(){let p=location.pathname.replace(/index.html$/,'');if(p.endsWith('/contabilidad/'))return'';let m=p.match(/\/contabilidad\/([^/]+)\//);return m?m[1]:''}
function ensureSidebarMenu(){let menu=document.querySelector('.sidebar .menu')||document.querySelector('.menu');if(!menu)return;let base=contabilidadRootPrefix(),active=currentSectionKey();menu.innerHTML=SIDEBAR_ITEMS.map(([key,icon,label])=>{let href=key===''?base:base+key+'/';let cls='menu-item'+(key===active?' active':'');return `<a class="${cls}" href="${href}" data-fade><span class="mi mi-icon">${icon}</span><span>${label}</span></a>`}).join('')}
function setServerStatus(text,ok=false){document.querySelectorAll('.brand-status,.brand-subtitle').forEach(el=>{el.innerHTML=`<span class="dot status-dot"></span>${text}`;if(ok)el.classList.add('server-ok');else el.classList.remove('server-ok')})}
function markServerOk(){localStorage.removeItem('contabilidad_sync_error');setServerStatus('Servidor activo',true)}
function markServerLocal(msg){if(msg)localStorage.setItem('contabilidad_sync_error',msg);setServerStatus('Modo local',false)}
aplicarModoVisual();
function authData(){try{return JSON.parse(localStorage.getItem(DIRECTUS_AUTH_KEY)||localStorage.getItem('pocketbase_auth')||'null')||{}}catch(e){return{}}}
function tokenExpired(t){try{let p=JSON.parse(atob(t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')));return p.exp&&p.exp*1000<Date.now()}catch(e){return true}}
function isLoginPage(){return location.pathname.includes('/contabilidad/login/')}
function loginUrl(){let rel=(location.pathname.split('/contabilidad/')[1]||'');return rel===''||rel==='index.html'?'login/':'../login/'}
function homeUrl(){let before=location.pathname.split('/contabilidad/')[0]||'';return (before.endsWith('/')?before:before+'/')||'/'}
function isAuthenticated(){let a=authData();return !!(a.token&&!tokenExpired(a.token))}
function requireAuth(){if(isLoginPage())return true;if(!isAuthenticated()){location.href=loginUrl();return false}return true}
function userLabel(){let m=authData().model||{};return m.email||m.username||m.name||'Usuario'}
function fadeTo(url){document.body.classList.add('page-leaving');setTimeout(()=>{location.href=url},180)}
function logoutContabilidad(){localStorage.removeItem(DIRECTUS_AUTH_KEY);localStorage.removeItem('pocketbase_auth');localStorage.removeItem(SERVER_ID_KEY);fadeTo(homeUrl())}
requireAuth();
function seed(){return{atenciones:[],usuarios:[],profesionales:[],aranceles:[{id:uid(),servicio:'Psicología',tramo:'General',valor:0,obs:'Completar valor'},{id:uid(),servicio:'Terapia Ocupacional',tramo:'General',valor:0,obs:'Completar valor'},{id:uid(),servicio:'Fonoaudiología',tramo:'General',valor:0,obs:'Completar valor'},{id:uid(),servicio:'Psicopedagogía',tramo:'General',valor:0,obs:'Completar valor'},{id:uid(),servicio:'ADOS-2 / ADI-R',tramo:'Evaluación',valor:0,obs:'Completar valor'}],egresos:[],cierres:[]}}
function fixDb(db){db=db||seed();db.atenciones=db.atenciones||[];db.usuarios=db.usuarios||[];db.profesionales=db.profesionales||[];db.aranceles=db.aranceles||[];db.egresos=db.egresos||[];db.cierres=db.cierres||[];db.atenciones=db.atenciones.map(a=>({...a,liquidado:!!a.liquidado,boleta:a.boleta||'pendiente'}));return db}
function loadLocal(){let raw=localStorage.getItem(KEY)||localStorage.getItem(OLD);return raw?fixDb(JSON.parse(raw)):seed()}
function load(){let db=loadLocal();setTimeout(syncFromServer,250);return db}
function save(db){db=fixDb(db);localStorage.setItem(KEY,JSON.stringify(db));saveToServer(db)}
function serverHeaders(){return{'Content-Type':'application/json','Authorization':'Bearer '+authData().token}}
async function directusJson(path,options={}){let res=await fetch(DIRECTUS_URL+path,{...options,headers:{...serverHeaders(),...(options.headers||{})}});let txt=await res.text();let data=null;try{data=txt?JSON.parse(txt):null}catch(e){data=txt}if(!res.ok){let msg=data&&data.errors&&data.errors[0]?data.errors[0].message:res.status;throw new Error('Directus: '+msg)}return data}
async function findServerRecord(){let path=`/items/${SERVER_COLLECTION}?filter[clave][_eq]=${encodeURIComponent(SERVER_KEY)}&limit=1`;let data=await directusJson(path);return data.data&&data.data[0]?data.data[0]:null}
async function readServer(){if(!isAuthenticated()||isLoginPage())return null;let rec=await findServerRecord();if(!rec){let local=loadLocal();await createServer(local);return local}localStorage.setItem(SERVER_ID_KEY,rec.id);markServerOk();return fixDb(rec.datos||seed())}
async function createServer(db){let data=await directusJson(`/items/${SERVER_COLLECTION}`,{method:'POST',body:JSON.stringify({clave:SERVER_KEY,datos:fixDb(db)})});let rec=data.data;localStorage.setItem(SERVER_ID_KEY,rec.id);markServerOk();return rec}
async function saveToServer(db){if(!isAuthenticated()||isLoginPage())return;try{let id=localStorage.getItem(SERVER_ID_KEY);if(!id){let rec=await findServerRecord();if(rec){id=rec.id;localStorage.setItem(SERVER_ID_KEY,id)}}let body=JSON.stringify({clave:SERVER_KEY,datos:fixDb(db)});let data=id?await directusJson(`/items/${SERVER_COLLECTION}/${id}`,{method:'PATCH',body}):await directusJson(`/items/${SERVER_COLLECTION}`,{method:'POST',body});let rec=data.data;localStorage.setItem(SERVER_ID_KEY,rec.id);markServerOk()}catch(e){console.error(e);markServerLocal(e.message)}}
async function syncFromServer(){if(!isAuthenticated()||isLoginPage())return;try{let server=await readServer();if(!server)return;let serverRaw=JSON.stringify(server),localRaw=JSON.stringify(loadLocal());if(serverRaw!==localRaw){localStorage.setItem(KEY,serverRaw);let once='contabilidad_hydrated_'+location.pathname;if(sessionStorage.getItem(once)!=='1'){sessionStorage.setItem(once,'1');location.reload()}}markServerOk()}catch(e){console.error(e);markServerLocal(e.message)}}
async function sincronizarContabilidad(){let db=loadLocal();await saveToServer(db);await syncFromServer();return loadLocal()}
function cob(a){if(a.estado==='pagado')return n(a.arancel);if(a.estado==='abonado')return Math.min(n(a.abono),n(a.arancel));return 0}
function deuda(a){return Math.max(n(a.arancel)-cob(a),0)}
function calc(a){let c=cob(a),inst=c*n(a.porcentaje||30)/100,base=Math.max(c-inst,0),ret=base*n(a.retencion||15.25)/100;return{c,inst,base,ret,liq:Math.max(base-ret,0),deuda:deuda(a)}}
function byPeriod(db,mes='',q='todas',pro='todos'){return db.atenciones.filter(a=>{if(mes&&!(a.fecha||'').startsWith(mes))return false;let d=n((a.fecha||'').slice(8,10));if(q==='1'&&d>15)return false;if(q==='2'&&d<16)return false;if(pro!=='todos'&&a.profesional!==pro)return false;return true})}
function egByPeriod(db,mes='',q='todas'){return db.egresos.filter(e=>{if(mes&&!(e.fecha||'').startsWith(mes))return false;let d=n((e.fecha||'').slice(8,10));if(q==='1'&&d>15)return false;if(q==='2'&&d<16)return false;return true})}
function resumen(ats,egs=[]){let r={bruto:0,cobrado:0,inst:0,prof:0,deuda:0,pendLiq:0,n:ats.length};ats.forEach(a=>{let c=calc(a);r.bruto+=n(a.arancel);r.cobrado+=c.c;r.inst+=c.inst;r.prof+=c.liq;r.deuda+=c.deuda;if(!a.liquidado)r.pendLiq+=c.liq});r.egresos=egs.reduce((s,e)=>s+n(e.monto),0);r.saldo=r.inst-r.egresos;return r}
function nav(prefix=''){let root=prefix||'./';return `<nav class="top-links"><a href="${root}" data-nav>Panel</a><a href="${root}atenciones/" data-nav>Atenciones</a><a href="${root}caja/" data-nav>Caja</a><a href="${root}deudas/" data-nav>Deudas</a><a href="${root}liquidaciones/" data-nav>Pagos prof.</a><a href="${root}egresos/" data-nav>Egresos</a><details class="nav-more"><summary>Config.</summary><div><a href="${root}usuarios/" data-nav>Usuarios</a><a href="${root}profesionales/" data-nav>Profesionales</a><a href="${root}aranceles/" data-nav>Aranceles</a><a href="${root}reportes/" data-nav>Reportes</a></div></details></nav>`}
function sidebar(prefix=''){let err=localStorage.getItem('contabilidad_sync_error');return `<aside class="side"><div class="brand"><div class="logo">$</div><div class="brand-text"><span class="brand-title">Contabilidad</span><span class="brand-subtitle">${err?'Modo local':'Servidor activo'}</span></div></div>${nav(prefix)}<div class="auth-mini"><button class="visual-btn" onclick="toggleModoVisual()">Modo visual</button><span>${esc(userLabel())}</span><button onclick="logoutContabilidad()">Salir</button></div></aside>`}
function initDates(){document.querySelectorAll('[data-today]').forEach(x=>x.value=today());document.querySelectorAll('[data-month]').forEach(x=>x.value=month())}
function fillLists(db){document.querySelectorAll('[data-pros]').forEach(el=>{let a=[...new Set([...db.profesionales.map(x=>x.nombre),...db.atenciones.map(x=>x.profesional)].filter(Boolean))].sort();el.innerHTML=a.map(x=>`<option value="${esc(x)}">`).join('')});document.querySelectorAll('[data-users]').forEach(el=>{let a=[...new Set([...db.usuarios.map(x=>x.nombre),...db.atenciones.map(x=>x.usuario)].filter(Boolean))].sort();el.innerHTML=a.map(x=>`<option value="${esc(x)}">`).join('')});document.querySelectorAll('[data-servs]').forEach(el=>{let a=[...new Set([...db.aranceles.map(x=>x.servicio),...db.atenciones.map(x=>x.servicio)].filter(Boolean))].sort();el.innerHTML=a.map(x=>`<option value="${esc(x)}">`).join('')})}
function download(name,txt,type){let b=new Blob([txt],{type}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=name;a.click();URL.revokeObjectURL(u)}
function enhanceNavigation(){document.body.classList.add('page-ready');ensureSidebarMenu();ensureModoVisualButton();aplicarModoVisual();setServerStatus(localStorage.getItem('contabilidad_sync_error')?'Modo local':'Conectando...',false);let path=location.pathname.replace(/index.html$/,'');document.querySelectorAll('[data-nav]').forEach(a=>{let href=new URL(a.getAttribute('href'),location.href).pathname.replace(/index.html$/,'');if(href===path)a.classList.add('active');a.addEventListener('click',e=>{if(e.ctrlKey||e.metaKey)return;e.preventDefault();fadeTo(a.href)})});document.querySelectorAll('a[data-fade]').forEach(a=>{if(a.dataset.boundFade==='1')return;a.dataset.boundFade='1';a.addEventListener('click',e=>{if(e.ctrlKey||e.metaKey)return;e.preventDefault();fadeTo(a.href)})});document.querySelectorAll('.card,.stat,.table,.panel,.kpi').forEach((el,i)=>{el.style.animationDelay=(Math.min(i,8)*35)+'ms';el.classList.add('fade-item')})}
document.addEventListener('DOMContentLoaded',enhanceNavigation);
window.sincronizarContabilidad=sincronizarContabilidad;
